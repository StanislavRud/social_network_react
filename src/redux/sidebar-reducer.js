let initialState = {
    friendsData: [
        {id: 1, name: 'Stas'},
        {id: 2, name: 'Dimon'},
        {id: 3, name: 'Valera'}
    ]
};


const sidebarReducer = (state = initialState, action) => {



    return state;
};

export default sidebarReducer;
